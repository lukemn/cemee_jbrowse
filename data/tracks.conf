[WS220_genes]
key = WS220_genes
storeClass = JBrowse/Store/SeqFeature/NCList
type = CanvasFeatures
fmtDetailValue_Name = function(alias, feature) {
    /* only add links to the top-level feature */
    if(feature.get('type')=='gene') {
        return name + ' [<a href=https://wormbase.org/search/gene/'+name+'>Search WormBase</a>]';
    }
  }

